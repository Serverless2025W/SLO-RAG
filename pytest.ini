[pytest]
addopts = --import-mode=importlib
markers =
    integration: integration tests requiring external services
# Ignore test directories with missing optional dependencies
testpaths = query_embedding_retrieval conversation-manager context-summarizer text-extraction embedding-generation router redpanda-connector
norecursedirs = local-inference remote-inference .venv venv __pycache__ *.egg
